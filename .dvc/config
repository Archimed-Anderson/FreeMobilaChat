# Configuration DVC pour FreeMobilaChat
# Data Version Control - Gestion des datasets et modèles

# Stockage distant (choisir une option)
[remote "gdrive"]
    url = gdrive://1A2B3C4D5E6F7G8H9I0J
    # Google Drive pour académique (gratuit 15GB)

[remote "s3"]
    url = s3://freemobilachat-datasets
    region = eu-west-1
    # AWS S3 pour production (payant mais scalable)

[remote "local_backup"]
    url = /mnt/backup/freemobilachat
    # Backup local pour sécurité

# Remote par défaut
[core]
    remote = gdrive
    autostage = true
    
# Cache local
[cache]
    dir = .dvc/cache
    type = reflink,hardlink,symlink
    
# Configuration analytics
[analytics]
    enabled = false
    
# Configuration metrics
[metrics]
    precision = 4
    
# Configuration expériences
[exp]
    auto_push = false
